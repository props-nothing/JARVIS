# End-to-End Tests

This directory owns packaged, cross-process acceptance tests. It intentionally
does not contain an empty Cargo package; the first executable harness is added
with the Foundation behavior it proves.